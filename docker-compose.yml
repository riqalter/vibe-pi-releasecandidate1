services:
  backend:
    image: chatbot-backend:latest
    ports:
      - "8000:8000"
    env_file: "chatbot.env"

  frontend:
    image: chatbot-frontend:latest
    ports:
      - "8501:8501"
    env_file: "chatbot.env"
    depends_on:
      - backend
